server {
    listen 443 ssl http2;

    server_name {{ lookup('env', 'MAIL_SERVICE_DOMAIN') }};

    include snippets/mtls.conf;

    location / {
        proxy_pass http://127.0.0.1:{{ lookup('env', 'MAIL_SERVICE_PORT') }};
    }
}